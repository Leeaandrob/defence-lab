# Python
__pycache__/
*.pyc
*.egg-info/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# vendored source & model weights (large / fetched separately)
third_party/
checkpoints/*.pt
checkpoints/*.pth

# experiment run outputs (reproducible artifacts, not source)
experiments/*
!experiments/.gitkeep

# run logs & generated dashboard (not source)
*.log
dashboard/
data/

# keep directory placeholders
!**/.gitkeep
